# Frontend de integración — Fase 3 (reservado)

> **Estado: Vacío.** Esta carpeta se implementará en la Fase 3 cuando los módulos `sensor` y `salto` estén completos.

Aquí se construirá el dashboard o SPA que unifique todos los módulos del proyecto en una sola interfaz web.

Mientras tanto, cada módulo tiene su propio frontend para desarrollo y pruebas en `modules/<nombre>/frontend/`.
